/*import express from 'express';
import path from 'path';

const app = express();
const PORT = 8080; // Puedes cambiar el puerto si es necesario

// Servir archivos estáticos desde la carpeta 'dist' en la ruta especificada
app.use(express.static('D:/Municipalidad de jaen/muni_ticket/dist'));

// Redirigir todas las rutas al 'index.html' para aplicaciones SPA
app.get('*', (req, res) => {
    res.sendFile('D:/Municipalidad de jaen/muni_ticket/dist/index.html');
});

// Iniciar el servidor
app.listen(PORT, () => {
    console.log(`Servidor corriendo en http://localhost:${PORT}`);
});*/